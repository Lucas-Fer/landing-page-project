import { styled } from '..'

export const HomeContainer = styled('main', {
  display: 'flex',
  flexDirection: 'column',

  border: '1px solid black',
  width: '60%',
  margin: '0 auto',
  padding: '1rem',
})
